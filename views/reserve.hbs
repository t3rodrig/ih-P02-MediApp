<div class="row">
    {{#each days}}
        <div class="col">
            <h4>{{@key}}</h4>
        </div>
    {{/each}}
</div>
<div class="row">
        {{#each days}}
        <div class="col">
            {{#each this}}
                <h4> <a href="#" role="button" class="btn btn-primary">{{this}}</a></h4>
            {{/each}}
        </div>
    {{/each}}
</div>